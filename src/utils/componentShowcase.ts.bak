/**
 * Component Showcase - Interactive demo page for all AegisShare v4 components
 * Used for development, testing, and documentation purposes
 */

export interface ComponentShowcaseItem {
    category: string
    name: string
    description: string
    component: React.ReactNode
    code: string
    notes?: string
}

export const showcaseItems: ComponentShowcaseItem[] = [
    // UI Components
    {
        category: 'UI Components',
        name: 'Tabs',
        description: 'Multi-section tabbed interface with variants',
        component: <div>See Tabs component implementation</ div >,
    code: `<Tabs
  items={[
    { id: 'tab1', label: 'Overview' },
    { id: 'tab2', label: 'Details' },
  ]}
  activeId="tab1"
  variant="underline"
/>`,
    },
{
    category: 'UI Components',
        name: 'Popover',
            description: 'Context menu with placement and trigger modes',
                component: <div>See Popover component implementation </div>,
    code: `<Popover
  trigger={<button>Click me</button>}
  content="Popover content"
  placement="top"
  trigger_type="click"
/>`,
    },
{
    category: 'UI Components',
        name: 'StatusPill',
            description: 'Status badge with 5 severity levels',
                component: <div>See StatusPill component implementation </div>,
    code: `<StatusPill
  status="critical"
  label="Critical Alert"
  animate={true}
/>`,
    },
{
    category: 'UI Components',
        name: 'Drawer',
            description: 'Slide-in side panel for details and forms',
                component: <div>See Drawer component implementation </div>,
    code: `<Drawer
  open={true}
  onClose={() => {}}
  position="right"
  title="Settings"
>
  <p>Drawer content here</p>
</Drawer>`,
    },
{
    category: 'UI Components',
        name: 'ProgressRing',
            description: 'SVG circular progress indicator',
                component: <div>See ProgressRing component implementation </div>,
    code: `<ProgressRing
  value={75}
  max={100}
  radius={45}
  color="var(--cy)"
/>`,
    },
{
    category: 'UI Components',
        name: 'FilterChip',
            description: 'Active/removable filter pills',
                component: <div>See FilterChip component implementation </div>,
    code: `<FilterChip
  label="Active Filters"
  active={true}
  removable={true}
  count={5}
/>`,
    },
{
    category: 'UI Components',
        name: 'Timeline',
            description: 'Step-by-step progress visualizer',
                component: <div>See Timeline component implementation </div>,
    code: `<Timeline
  steps={[
    { id: '1', step: 1, label: 'Step 1', status: 'completed' },
    { id: '2', step: 2, label: 'Step 2', status: 'in-progress' },
  ]}
  direction="horizontal"
/>`,
    },

// Page Components - Phase 2
{
    category: 'Pages - Phase 2',
        name: 'CommandCenter',
            description: 'SOC operations dashboard with KPI tracking',
                component: <div>See CommandCenter component </div>,
    code: `<CommandCenter
  defconLevel={2}
  kpis={[...]}
  swimlane={[...]}
  healthTiles={[...]}
/>`,
        notes: 'Bento grid layout with staggered animations',
    },
{
    category: 'Pages - Phase 2',
        name: 'ThreatCenter',
            description: 'Threat assessment with MITRE heatmap',
                component: <div>See ThreatCenter component </div>,
    code: `<ThreatCenter
  mitreMatrix={[...]}
  baselineData={[...]}
  geoMap={[...]}
  playbooks={[...]}
/>`,
        notes: 'Interactive MITRE matrix with baseline anomalies',
    },
{
    category: 'Pages - Phase 2',
        name: 'SecureVault',
            description: 'Zero-knowledge secret management',
                component: <div>See SecureVault component </div>,
    code: `<SecureVault
  files={[...]}
  stats={[...]}
  uploadWizard={[...]}
  detailPanel={[...]}
/>`,
        notes: 'Multi-tab detail panel with progress tracking',
    },

// Page Components - Phase 3
{
    category: 'Pages - Phase 3',
        name: 'AuditLedger',
            description: 'Merkle chain visualization with timeline',
                component: <div>See AuditLedger component </div>,
    code: `<AuditLedger
  merkleNodes={[...]}
  auditEntries={[...]}
  siemFormat="json"
/>`,
        notes: 'Immutable audit trail with SIEM export',
    },
{
    category: 'Pages - Phase 3',
        name: 'KeyManagement',
            description: 'Key hierarchy with rotation scheduling',
                component: <div>See KeyManagement component </div>,
    code: `<KeyManagement
  keyTree={[...]}
  rotationSchedule={[...]}
  hsmDevices={[...]}
/>`,
        notes: 'Interactive key tree with Gantt timeline',
    },
{
    category: 'Pages - Phase 3',
        name: 'AccessControl',
            description: 'User access matrix with risk scoring',
                component: <div>See AccessControl component </div>,
    code: `<AccessControl
  users={[...]}
  rbacMatrix={[...]}
  riskScores={[...]}
/>`,
        notes: 'JIT access request panel with approval workflow',
    },

// Page Components - Phase 4
{
    category: 'Pages - Phase 4',
        name: 'PolicyEngine',
            description: 'Visual rule builder with OPA editor',
                component: <div>See PolicyEngine component </div>,
    code: `<PolicyEngine
  rules={[...]}
  opaPolicy="package policy\\nallow = true"
  onExecute={handleSimulation}
/>`,
        notes: 'PDP simulator for decision testing',
    },
{
    category: 'Pages - Phase 4',
        name: 'ComplianceHub',
            description: 'Multi-framework compliance tracking',
                component: <div>See ComplianceHub component </div>,
    code: `<ComplianceHub
  frameworks={[...]}
  controls={[...]}
/>`,
        notes: 'Arc gauges for compliance scoring',
    },
{
    category: 'Pages - Phase 4',
        name: 'Configuration',
            description: 'Profile presets with diff viewer',
                component: <div>See Configuration component </div>,
    code: `<Configuration
  presets={[...]}
  currentSettings={[...]}
/>`,
        notes: 'Unsaved changes banner with diff view',
    },
{
    category: 'Pages - Phase 4',
        name: 'AdminConsole',
            description: 'System topology with SLA gauges',
                component: <div>See AdminConsole component </div>,
    code: `<AdminConsole
  slaMetrics={[...]}
  securityScores={[...]}
  drSteps={[...]}
/>`,
        notes: '5-step disaster recovery wizard',
    },

// Enhancements
{
    category: 'Enhancements',
        name: 'DashboardBuilder',
            description: 'Customizable dashboard layout system',
                component: <div>See DashboardBuilder component </div>,
    code: `<DashboardBuilder
  widgets={[...]}
  editable={true}
  onSave={handleSave}
/>`,
        notes: 'Drag-to-reorder widget layout',
    },
]

/**
 * Get showcase items by category
 */
export function getShowcasesByCategory(category: string) {
    return showcaseItems.filter((item) => item.category === category)
}

/**
 * Get all unique categories
 */
export function getCategories() {
    return Array.from(new Set(showcaseItems.map((item) => item.category)))
}

/**
 * Get single showcase by name
 */
export function getShowcase(name: string) {
    return showcaseItems.find((item) => item.name === name)
}

/**
 * Generate markdown documentation for all components
 */
export function generateMarkdownDocs(): string {
    let md = '# AegisShare v4 GodMode Component Gallery\n\n'

    getCategories().forEach((category) => {
        md += `## ${category}\n\n`
        getShowcasesByCategory(category).forEach((item) => {
            md += `### ${item.name}\n\n`
            md += `**Description:** ${item.description}\n\n`
            if (item.notes) {
                md += `**Notes:** ${item.notes}\n\n`
            }
            md += `**Usage:**\n\`\`\`jsx\n${item.code}\n\`\`\`\n\n`
        })
    })

    return md
}
